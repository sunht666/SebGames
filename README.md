<div align="center">

# 🎮 SebGames

**实时多人在线派对游戏平台**

[![Node.js](https://img.shields.io/badge/Node.js-18+-339933?logo=node.js&logoColor=white)](https://nodejs.org/)
[![Vue 3](https://img.shields.io/badge/Vue-3.4-4FC08D?logo=vue.js&logoColor=white)](https://vuejs.org/)
[![WebSocket](https://img.shields.io/badge/WebSocket-实时通信-blue?logo=websocket)](https://developer.mozilla.org/docs/Web/API/WebSocket)
[![License](https://img.shields.io/badge/License-MIT-yellow)](LICENSE)

一个轻量级、零依赖数据库的多人在线游戏平台，内置 5 款经典派对游戏。
基于 WebSocket 实现毫秒级实时同步，支持断线重连、观战、表情互动。

[快速开始](#-快速开始) · [游戏介绍](#-游戏介绍) · [技术架构](#-技术架构) · [部署指南](#-部署指南)

</div>

---

## ✨ 特性一览

- **5 款派对游戏** — 数字排雷、唬牌、猜瓶盖、德国心脏病、五子棋
- **实时多人对战** — 基于 WebSocket，延迟 < 50ms
- **断线重连** — 30 秒重连窗口，游戏状态完整恢复
- **观战系统** — 随时加入观看进行中的游戏
- **表情互动** — 点击头像发送 emoji 表情，所有玩家实时可见
- **踢人机制** — 房主可在等待阶段踢出玩家（30 秒 IP 封禁）
- **暗色主题** — 精心调校的深色 UI，长时间游戏不伤眼
- **移动端适配** — 响应式设计，手机平板均可流畅游玩
- **零数据库** — 纯内存运行，无需任何外部服务

---

## 🎯 游戏介绍

### 1. 数字排雷（Number Mine）

| 项目 | 详情 |
|------|------|
| 👥 人数 | 2 人 |
| ⏱ 回合时限 | 15 - 60 秒（默认 30 秒） |

双方各自秘密设定一个 4 位数字（1000-9999），然后轮流猜测对方的数字。每次猜测后会得到「命中数」反馈——即猜对了几个位置上的数字。先猜中对方全部 4 位数字的玩家获胜。

**游戏流程：** 加入房间 → 双方设定数字（60 秒） → 掷骰子决定先手 → 轮流猜数 → 猜中获胜

---

### 2. 唬牌（Bluff Card）

| 项目 | 详情 |
|------|------|
| 👥 人数 | 2 - 6 人 |
| ⏱ 回合时限 | 15 - 30 秒（默认 20 秒） |
| 🃏 牌组 | 1 - 4 副标准扑克牌 |

经典的吹牛扑克。出牌时声明你打出的牌面（可以说谎），其他玩家可以选择「质疑」或「不质疑」。被揭穿说谎者收回牌堆，质疑失败者收回牌堆。先出完手牌的玩家获胜。

**可选配置：**
- 牌组数量（1-4 副），多副牌增加博弈深度
- 洗牌模式：随机弃掉部分牌，防止数牌

---

### 3. 猜瓶盖（Bottle Cap）

| 项目 | 详情 |
|------|------|
| 👥 人数 | 2 - 6 人 |
| ⏱ 回合时限 | 6 - 30 秒（默认 12 秒） |

每轮由庄家秘密「藏」若干瓶盖（0 到 N 个），其他玩家依次猜测藏了几个。猜对则成为下一轮庄家，猜错则扣分。累计扣分达到上限的玩家被淘汰，最后存活的玩家获胜。

---

### 4. 德国心脏病（Halli Galli）

| 项目 | 详情 |
|------|------|
| 👥 人数 | 2 - 6 人 |
| ⏱ 翻牌时限 | 3 - 10 秒（默认 5 秒） |
| 🍌 牌组 | 56 张水果牌（香蕉/草莓/青柠/李子 × 数量 1-5） |

经典反应力桌游的数字化版本。玩家按顺序翻牌，当桌面上任意一种水果的总数恰好为 **5** 时，最快拍铃的玩家赢得所有桌面牌。手牌耗尽即被淘汰，最后持有手牌的玩家获胜。

---

### 5. 五子棋（Gomoku）

| 项目 | 详情 |
|------|------|
| 👥 人数 | 2 人 |
| ⏱ 回合时限 | 15 - 60 秒（默认 30 秒） |
| 📐 棋盘 | 15 × 15 |

经典策略棋类。双方在 15×15 棋盘上交替落子，先在横、纵或斜方向连成 5 子的一方获胜。

**特殊功能：**
- **悔棋系统** — 落子后 5 秒内可申请悔棋，需对手同意（10 秒超时自动拒绝）
- 掷骰子决定执黑先手

---

## 🏗 技术架构

```
┌─────────────────────────────────────────────────┐
│                   客户端 (Client)                 │
│  Vue 3 + Vue Router + Vite                       │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐      │
│  │ Home.vue  │ │ Game.vue  │ │PlayerBar  │      │
│  │ 大厅/房间  │ │ ×5 游戏   │ │ 表情互动   │      │
│  └───────────┘ └───────────┘ └───────────┘      │
│              WebSocket + REST API                 │
└──────────────────────┬──────────────────────────┘
                       │
           ┌───────────▼───────────┐
           │     服务端 (Server)     │
           │  Node.js + Express     │
           │  ┌─────────────────┐  │
           │  │   WebSocket     │  │
           │  │  (ws library)   │  │
           │  └────────┬────────┘  │
           │  ┌────────▼────────┐  │
           │  │   BaseGame      │  │
           │  │  ┌──────────┐   │  │
           │  │  │NumberMine│   │  │
           │  │  │BluffCard │   │  │
           │  │  │BottleCap │   │  │
           │  │  │HalliGalli│   │  │
           │  │  │ Gomoku   │   │  │
           │  │  └──────────┘   │  │
           │  └─────────────────┘  │
           └───────────────────────┘
```

### 技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| **前端框架** | Vue 3 (Composition API) | 3.4 |
| **前端路由** | Vue Router | 4.3 |
| **构建工具** | Vite | 4.5 |
| **后端运行时** | Node.js | 14+（推荐 18+） |
| **HTTP 框架** | Express | 4.18 |
| **WebSocket** | ws | 8.16 |
| **并发工具** | concurrently | 8.2 |

### 项目结构

```
SebGames/
├── client/                     # 前端
│   ├── src/
│   │   ├── views/              # 页面组件
│   │   │   ├── Home.vue        # 游戏大厅
│   │   │   ├── NumberMine.vue  # 数字排雷
│   │   │   ├── BluffCard.vue   # 唬牌
│   │   │   ├── BottleCap.vue   # 猜瓶盖
│   │   │   ├── HalliGalli.vue  # 德国心脏病
│   │   │   └── Gomoku.vue      # 五子棋
│   │   ├── components/
│   │   │   └── PlayerBar.vue   # 玩家头像栏 + 表情
│   │   ├── router/index.js     # 路由配置
│   │   ├── assets/style.css    # 全局样式 + 设计系统
│   │   ├── App.vue
│   │   └── main.js
│   ├── vite.config.js
│   └── package.json
├── server/                     # 后端
│   ├── index.js                # 入口：HTTP + WebSocket + 房间管理
│   ├── games/
│   │   ├── BaseGame.js         # 游戏基类（房间/观战/重连/广播）
│   │   ├── NumberMine.js
│   │   ├── BluffCard.js
│   │   ├── BottleCap.js
│   │   ├── HalliGalli.js
│   │   ├── Gomoku.js
│   │   └── index.js            # 游戏工厂注册
│   └── package.json
└── package.json                # 根配置（scripts 入口）
```

### 核心机制

#### WebSocket 通信
- **心跳检测** — 每 25 秒 ping/pong，自动清理死连接
- **消息限流** — WebSocket 每秒最多 10 条消息，HTTP 每分钟 120 次
- **状态广播** — 所有游戏状态变更实时推送给玩家和观众

#### 房间系统
- 4 位数字房间号（1000-9999）
- 空房间 60 秒后自动清理
- 游戏结束 10 秒后自动解散
- 大厅实时显示活跃房间列表

#### 断线重连
- 游戏进行中断线后有 30 秒重连窗口
- 重连时完整恢复游戏状态（手牌、棋盘、计时器等）
- 断线期间其他玩家收到「玩家离线」提示
- 超时未重连则自动移除

---

## 🚀 快速开始

### 环境要求

- [Node.js](https://nodejs.org/) >= 14（推荐 18+）
- npm（随 Node.js 一起安装）

### 安装与运行

```bash
# 1. 克隆项目
git clone https://github.com/sunht666/SebGames.git
cd SebGames

# 2. 安装所有依赖（根目录 + server + client）
npm run install:all

# 3. 开发模式启动（前后端同时启动，支持热更新）
npm run dev
```

启动后访问 **http://localhost:5173** 即可开始游戏。

### 各平台 Node.js 安装指引

<details>
<summary><b>macOS</b></summary>

```bash
# 方式一：Homebrew（推荐）
brew install node

# 方式二：官网下载
# 访问 https://nodejs.org/ 下载 macOS 安装包

# 验证安装
node -v   # 应输出 v18.x.x 或更高
npm -v
```

</details>

<details>
<summary><b>Windows</b></summary>

```powershell
# 方式一：winget（Windows 10/11）
winget install OpenJS.NodeJS.LTS

# 方式二：官网下载
# 访问 https://nodejs.org/ 下载 Windows 安装包（.msi）
# 安装时勾选 "Add to PATH"

# 方式三：scoop
scoop install nodejs-lts

# 验证安装（打开 PowerShell 或 CMD）
node -v
npm -v
```

</details>

<details>
<summary><b>Linux (Ubuntu / Debian)</b></summary>

```bash
# 方式一：NodeSource（推荐，获取最新 LTS）
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# 方式二：系统包管理器（版本可能较旧）
sudo apt update
sudo apt install nodejs npm

# 方式三：nvm（多版本管理）
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
source ~/.bashrc
nvm install 18
nvm use 18

# 验证安装
node -v
npm -v
```

</details>

<details>
<summary><b>Linux (CentOS / RHEL / Fedora)</b></summary>

```bash
# Fedora
sudo dnf install nodejs

# CentOS / RHEL（通过 NodeSource）
curl -fsSL https://rpm.nodesource.com/setup_18.x | sudo bash -
sudo yum install -y nodejs

# 验证安装
node -v
npm -v
```

</details>

---

## 📦 可用脚本

| 命令 | 说明 |
|------|------|
| `npm run install:all` | 安装所有依赖（根 + server + client） |
| `npm run dev` | 开发模式：同时启动前后端（热更新） |
| `npm run dev:server` | 仅启动后端（端口 3000） |
| `npm run dev:client` | 仅启动前端（端口 5173） |
| `npm run build` | 构建前端生产版本 → `client/dist/` |
| `npm start` | 生产模式启动（服务端托管前端静态文件） |

---

## 🌐 部署指南

### 生产部署

```bash
# 1. 构建前端
npm run build

# 2. 启动服务（默认端口 3000）
npm start

# 自定义端口
PORT=8080 npm start
```

生产模式下，服务端会自动托管 `client/dist/` 中的静态文件，只需暴露一个端口即可。

### 使用 PM2 守护进程

```bash
# 安装 PM2
npm install -g pm2

# 启动服务
pm2 start server/index.js --name sebgames

# 查看状态
pm2 status

# 查看日志
pm2 logs sebgames

# 开机自启
pm2 startup
pm2 save
```

### Docker 部署

```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY . .
RUN npm run install:all && npm run build
EXPOSE 3000
CMD ["npm", "start"]
```

```bash
docker build -t sebgames .
docker run -d -p 3000:3000 --name sebgames sebgames
```

### Nginx 反向代理（可选）

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

> **注意：** WebSocket 需要 `Upgrade` 和 `Connection` 头，务必保留以上配置。

---

## 🔧 环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `PORT` | `3000` | 服务端监听端口 |

---

## 📐 端口说明

| 服务 | 端口 | 协议 | 说明 |
|------|------|------|------|
| 前端开发服务器 | 5173 | HTTP | Vite 热更新服务 |
| 后端服务 | 3000 | HTTP / WS | Express + WebSocket |
| 生产环境 | 3000 | HTTP / WS | 前后端统一端口 |

开发模式下，Vite 会自动将 `/ws` 和 `/api` 请求代理到后端 3000 端口。

---

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

1. Fork 本仓库
2. 创建特性分支：`git checkout -b feat/amazing-feature`
3. 提交改动：`git commit -m 'feat: 添加了不起的功能'`
4. 推送分支：`git push origin feat/amazing-feature`
5. 提交 Pull Request

---

## 📄 License

[MIT](LICENSE)

---

<div align="center">
<sub>Built with ❤️ by SebGames Team</sub>
</div>
